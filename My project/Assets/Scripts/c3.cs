using UnityEngine;

public class c3 : MonoBehaviour
{

    // Start is called once before the first execution of Update after the MonoBehaviour is created

    void Start ()
    {
        c1.miObjeto.name = "newnameBitch";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
